# NLP-Project
To reproduce the results of the paper, run pip install -r requirements.txt. Then simply run extract_text.py (loads the data), then main.py then berttopic by parties.py (output the charts)
